# Backend Port Change: 8000 → 7007 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change the backend server port from 8000 to 7007 across all project files.

**Architecture:** Direct string replacement in 4 files — config, default value, frontend client, and startup script. No architectural changes.

**Tech Stack:** YAML config, Python (Pydantic), TypeScript (Vite frontend), Windows batch script

---

### Task 1: Update backend config.yaml

**Files:**
- Modify: `backend/config.yaml:3`

- [ ] **Step 1: Change port value**

In `backend/config.yaml`, line 3, change:
```yaml
  port: 8000
```
to:
```yaml
  port: 7007
```

- [ ] **Step 2: Verify config loads**

Run: `cd backend && python -c "from app.config import load_config; c = load_config(); assert c.server.port == 7007, f'Expected 7007, got {c.server.port}'; print('OK: port =', c.server.port)"`
Expected: `OK: port = 7007`

- [ ] **Step 3: Commit**

```bash
git add backend/config.yaml
git commit -m "config: change backend port from 8000 to 7007"
```

---

### Task 2: Update config.py default value

**Files:**
- Modify: `backend/app/config.py:40`

- [ ] **Step 1: Change default value**

In `backend/app/config.py`, line 40, change:
```python
    port: int = 8000
```
to:
```python
    port: int = 7007
```

- [ ] **Step 2: Verify default loads without config file**

Run: `cd backend && python -c "from app.config import ServerConfig; s = ServerConfig(); assert s.port == 7007, f'Expected 7007, got {s.port}'; print('OK: default port =', s.port)"`
Expected: `OK: default port = 7007`

- [ ] **Step 3: Commit**

```bash
git add backend/app/config.py
git commit -m "config: update ServerConfig default port to 7007"
```

---

### Task 3: Update frontend API client

**Files:**
- Modify: `frontend/src/api/client.ts:1-2`

- [ ] **Step 1: Change API_BASE and WS_URL**

In `frontend/src/api/client.ts`, lines 1-2, change:
```typescript
const API_BASE = 'http://localhost:8000';
const WS_URL = 'ws://localhost:8000/ws';
```
to:
```typescript
const API_BASE = 'http://localhost:7007';
const WS_URL = 'ws://localhost:7007/ws';
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `cd frontend && npx tsc --noEmit`
Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api/client.ts
git commit -m "config: update frontend API client to use port 7007"
```

---

### Task 4: Update start.bat

**Files:**
- Modify: `start.bat:10-11`

- [ ] **Step 1: Change port in start.bat**

In `start.bat`, lines 10-11, change:
```bat
echo [1/2] Starting backend (port 8000)...
start "Backend" /D "%ROOT%backend" cmd /k "py -3.12 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
```
to:
```bat
echo [1/2] Starting backend (port 7007)...
start "Backend" /D "%ROOT%backend" cmd /k "py -3.12 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 7007"
```

- [ ] **Step 2: Commit**

```bash
git add start.bat
git commit -m "config: update start.bat to use port 7007"
```

---

### Task 5: Final verification

- [ ] **Step 1: Run backend tests**

Run: `cd backend && python -m pytest tests/ -v`
Expected: all tests pass

- [ ] **Step 2: Grep for stale port references**

Run: `grep -r "8000" backend/config.yaml backend/app/config.py frontend/src/api/client.ts start.bat`
Expected: no matches (all 8000 references removed)
